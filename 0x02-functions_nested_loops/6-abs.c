#include "main.h"
/**
 * _abs- returns the absolute value of an interger@
 *
 * @n: the int to check
 * Return: the absolute value of int
 */
int_abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
